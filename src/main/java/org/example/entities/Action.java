package org.example.entities;

//* Enum con las acciones que puede contener el json que recibimos
public enum Action {
    SEND_MESSAGE,
    CLOSE_WINDOW,
    CREATE_CHAT_ROOM,
    CHANGE_CHAT_ROOM
}
