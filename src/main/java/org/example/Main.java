package org.example;

import org.example.services.HandlerClient;
import org.example.socket.SocketTcpServer;

public class Main {
    public static void main(String[] args) {
        HandlerClient handlerClient = HandlerClient.getInstance();
    }
}